import api from "../api";
// import Csrf from "../Csrf";

const state = {
  //
};

const getters = {
  //
};

const actions = {
  //
};

const mutations = {
  //
};

export const employees = {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
